import type { Meta, StoryObj } from '@storybook/react'
import { Toast } from '@call-sckedule/react'

export default {
  title: 'Form/Toast',
  component: Toast,
  args: {},
  tags: ['autodocs'],
} as Meta

export const Primary: StoryObj = {}
