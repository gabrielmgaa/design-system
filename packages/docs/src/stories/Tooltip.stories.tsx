import type { Meta, StoryObj } from '@storybook/react'
import { Tooltip } from '@call-sckedule/react'

export default {
  title: 'Form/Tooltip',
  component: Tooltip,
  args: {},
  tags: ['autodocs'],
} as Meta

export const Primary: StoryObj = {}
